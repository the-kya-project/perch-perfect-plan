{
  // GENERATED by scripts/i18n-translate.mjs — Dutch email catalog (namespace:
  // "emails"). Server-only; never shipped to the browser.
  //
  // Intentionally empty for now: Dutch email strings are generated at release
  // via `npm run i18n:translate` (which now covers this catalog too). Until then
  // every key falls back to English (fallbackLng), so all emails still send —
  // just in English — with no failures.
}
