# Mobile resubmission QA — September 16, 2026

Status: initial unauthenticated iOS simulator smoke testing completed; authenticated app flows and full device coverage pending. One dedicated production test account created with explicit user authorization; retain for testing and clean up later.

## Dedicated account and cleanup ledger

- Display name: `ZZ Codex Mobile QA`.
- Email: `zz-codex-mobile-qa-20260916-c3b0c9@example.com` (reserved example domain; no usable inbox).
- Auth user ID: `95a112d1-8fad-42fa-ab15-5efc67dc953c`.
- Credentials: private local file `/Users/brittanyking/.codex/private/perch-mobile-qa-2026-09-16.json`, mode 0600, outside the repo. Never commit its contents.
- Admin-created with confirmed email and marketing opt-in false. Password sign-in through Supabase Auth succeeded and returned the expected user ID. This is account provisioning verification, not a test of UI signup, email delivery, or Apple/Google signup.
- No birds or media created by this agent yet. Log exact IDs as testing adds data. User explicitly requested retaining the account for later cleanup.
- User confirmed two agents are testing concurrently. Coordinate code changes; this agent did not author the concurrent `nativeOAuth.ts` edit.

## Review target

Apple submission `947bbc37-ef4b-4225-9b00-f6e149dccea4` was rejected under 2.1(a): Apple sign-up returned an error. Review target: version 1.0 (4), iPhone 17 Pro Max, iOS 27.0, active internet. Exact error text is not yet available.

Local checkout: `271a426`. Xcode project specifies version 1.0 (3); Android specifies versionName 1.0, versionCode 2. Both shells load https://app.thekyaproject.com. Local web changes do not reach those shells without an explicit test configuration or deployment.

## Observed checks

- Repository initially clean except existing untracked `appstore-screenshots/`.
- Read CLAUDE.md and saved clip-lifecycle, RLS silent-write, and native-app memories. Historical memory claims are not current test results.
- `npm run lint`: exit 0, 0 errors, 28 warnings. Log: `/tmp/perch-mobile-qa-lint.log`.
- Xcode simulator inventory: iOS 26.5 iPhones and iPads available; no iOS 27.0 runtime in returned inventory.
- Xcode device inventory: registered iPhone 17 Pro Max unavailable.
- Follow-up after connection: iPhone 17 Pro Max is paired and available, running iOS 26.6.2. Developer Mode is disabled; `devicectl device info apps` failed with error 10005, so installed Kya build could not be read. User confirms Kya is available in TestFlight, build unknown. No dedicated test Apple ID currently available.
- After user enabled Developer Mode, `devicectl device info apps` succeeded and confirmed installed Kya version 1.0, bundle version 4. User independently confirmed 1.0 (4) in TestFlight. Physical-phone installation was not modified or used to access a personal account.
- A fresh isolated simulator, `Kya QA 2026-09-16` (`20FD806D-B92E-4366-966E-E38FEF6E63A7`), runs iPhone 17 Pro Max / iOS 26.5. Current-checkout unsigned Debug build succeeded (exit 0), installed, and launched. Build output: `/tmp/perch-mobile-qa-build`; build log: `/tmp/perch-mobile-qa-xcode.log`.
- Simulator landing screen rendered after an initial blank screen; tapping Sign in opened the expected native-aware auth page with Google, Apple, and email choices.
- Tapping Apple in the unsigned simulator without an Apple account reached Apple's authorization framework. Native logs reported AKAuthenticationError -7026 and ASAuthorizationController error 1000. This environment result does not reproduce or explain the rejected signed TestFlight build's failure. No Apple token exchange or successful signup was verified.
- Read-only production auth configuration: Apple and Google providers enabled; Apple client ID allowlist contains `com.thekyaproject.app` twice. Duplication is an observation, not a proven cause. No settings changed.
- Auth log query for Apple/error messages during September 15–16 returned zero rows. This is not proof that authentication succeeded or that no failures occurred.
- Android emulator launched in read-only/no-snapshot-save mode, Android 15, ADB `emulator-5554`. Its pre-existing app is an older debug installation and is not accepted as evidence for the current Play submission.
- Android emulator inventory: `kya_pixel`; ADB returned no attached devices.
- Apple sign-in entitlement is present in source. Entitlement and provisioning in submitted build 4 remain unverified.

## Source finding requiring follow-up

The installed social-login dependency's `AppleProvider.swift` prints serialized Apple tokens in `persistState` and `retrieveState`, and stores them in UserDefaults. This is confirmed in installed dependency source, not yet in submitted build 4 or runtime logs. Avoid capturing unredacted authentication logs. This finding does not establish the cause of the sign-up rejection.

## Execution matrix — all pending

Record device, OS, actual build, account category, steps, expected/actual results, and sanitized evidence for each run. Mark blocked and untested explicitly.

| Area | Cases |
| --- | --- |
| Installation | Exact rejected TestFlight build; clean install; update installation; cold launch; resume |
| Apple authentication | First authorization; share email; hide email; returning sign-in; cancellation; retry; sign-out; relaunch |
| Google authentication | First/returning sign-in; cancellation; retry; Play-installed signing identity |
| Email authentication | Sign-up; confirmation; sign-in; password reset; session expiry |
| Onboarding | Welcome; required-field validation; create test bird; navigation and persistence |
| Care plans | All setup sections; save/reload; sitter preview; private share access |
| Records | Weight; journal; scans; photos; edit/reload verification |
| Native media | Camera/microphone permission allowed and denied; photo picker; clip recording/upload/playback |
| Sharing and handoff | Only test-owned records and controlled recipient accounts; request authorization before sending invitations |
| Lifecycle | Background/resume; interrupted network; double taps; keyboard; Android back; orientation; iPad layout |
| Removal | Only rows/accounts created during this run; stub browser confirm before automation; verify database and media outcomes |

## Constraints and prerequisites

- Connect/unlock the iPhone and identify its OS and available TestFlight build.
- Identify provenance/artifact for submitted build 4.
- User enters dedicated test Apple/Google credentials; never use personal app account or paste passwords into reports.
- Android emulator testing is feasible; physical Android and Play-distributed behavior remain a coverage gap until a device/tester is available.
- Never delete pre-existing demo data. Keep an exact ledger of newly created rows/media and clean up only that ledger.
- Verify user-client updates/deletes against affected rows and database state, not UI toasts.
- Existing orphan cleanup and known open edge-case fixes remain out of scope without explicit approval.
- No push, deployment, store submission, or production configuration changes authorized by this test pass.
